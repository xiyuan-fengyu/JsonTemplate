{
    "a": "#{'id = ' + $('id')}",
    "#{define(id)}": 1,
    "b": "#{'id = ' + id}",
    "sub": {
        "c": "#{'id = ' + id}",
        "#{define(id)}": 2,
        "d": "#{'id = ' + id}"
    },
    "e": "#{'id = ' + id}"
}