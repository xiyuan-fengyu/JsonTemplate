{
    "a": "#{'id = ' + $('id')}",
    "#{set(id)}": 1,
    "b": "#{'id = ' + id}",
    "sub": {
        "c": "#{'id = ' + id}",
        "#{set(id)}": 2,
        "d": "#{'id = ' + id}"
    },
    "e": "#{'id = ' + id}",
    "#{set(arr)}": [],
    "f": "#{arr.slice()}",
    "#{set(useless)}": {
        "#{for (index, item) of [1, 2, 3]}": "#{arr.push(item)}"
    },
    "g": "#{arr.slice()}"
}